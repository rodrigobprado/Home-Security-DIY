# =============================================================================
# Kustomize Base - Home Security DIY
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: home-security

resources:
  - namespace.yaml
  - postgres/postgres.yaml
  - mosquitto/mosquitto.yaml
  - redis/redis.yaml
  - zigbee2mqtt/zigbee2mqtt.yaml
  - frigate/frigate.yaml
  - homeassistant/homeassistant.yaml
  - dashboard/dashboard.yaml
  - dashboard/ha-worker.yaml
  - pdb.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: home-security
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false

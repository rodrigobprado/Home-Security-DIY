# MQTT TLS Certificates

Place production certificates in this directory when enabling TLS listener `8883`.

Required files:
- `ca.crt`
- `server.crt`
- `server.key`

Never commit private keys with real production values.
